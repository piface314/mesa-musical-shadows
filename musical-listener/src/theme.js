import { StyleSheet } from 'react-native';

export const colors = {
  roselight: '#ff769c',
  rosepulse: '#ff5d91',
  purplelink: '#c393ff',
  bluesoft: '#006aff',
  bluedeep: '#1b45ff'
};

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  }
});